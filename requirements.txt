
pandas==1.3.3  # Pour la manipulation et l'analyse des données
beautifulsoup4==4.10.0  # Pour l'analyse HTML et le scraping web
requests==2.26.0  # Pour effectuer des requêtes HTTP
# pip install transformers
pip install spacy
python -m spacy download fr_core_news_sm
pip install sentencepiece
python -m pip install setfit
pip install tqdm